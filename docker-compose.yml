version: '3'

services:
  
  smtp:
    build: .
    image: smtp:ready
    container_name: smtp
    ports:
      - "25:25"
    volumes:
      - ./messages:/tmp

  node:
    image: ericminio/node
    container_name: node
    volumes:
      - .:/home/dev/app
    working_dir: /home/dev/app
